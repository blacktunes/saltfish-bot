{
  "name": "saltfish-bot",
  "version": "0.0.3",
  "main": "dist/index.js",
  "scripts": {
    "type-check": "tsc --noEmit -p tsconfig.json --composite false",
    "format": "prettier --write . --ignore-path .gitignore",
    "build": "rimraf dist && tsc && gulp copy"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.2",
    "@types/lodash": "^4.14.198",
    "@types/named-regexp-groups": "^1.0.1",
    "@types/node-schedule": "^2.1.0",
    "@types/websocket": "^1.0.6"
  },
  "dependencies": {
    "colors": "^1.4.0",
    "fs-extra": "^11.1.1",
    "lodash": "^4.17.21",
    "moment": "^2.29.4",
    "named-regexp-groups": "^1.0.6",
    "node-schedule": "^2.1.1",
    "node-yaml": "^4.0.1",
    "websocket": "^1.0.34"
  }
}
