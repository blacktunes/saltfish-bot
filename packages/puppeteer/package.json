{
  "name": "@saltfish-bot/puppeteer",
  "version": "0.0.3",
  "main": "dist/index.js",
  "scripts": {
    "type-check": "tsc --noEmit -p tsconfig.json --composite false",
    "format": "prettier --write . --ignore-path .gitignore",
    "build": "rimraf dist && tsc"
  },
  "dependencies": {
    "moment": "^2.29.4",
    "puppeteer": "^21.2.1"
  }
}
