{
  "name": "@saltfish-bot/plugin-system",
  "version": "0.0.2",
  "main": "dist/index.js",
  "scripts": {
    "type-check": "tsc --noEmit -p tsconfig.json --composite false",
    "format": "prettier --write . --ignore-path .gitignore",
    "build": "rimraf dist && tsc && gulp copy"
  },
  "dependencies": {
    "@saltfish-bot/puppeteer": "workspace:^",
    "node-os-utils": "^1.3.7",
    "saltfish-bot": "workspace:^"
  },
  "devDependencies": {
    "@types/node-os-utils": "^1.3.3"
  }
}
